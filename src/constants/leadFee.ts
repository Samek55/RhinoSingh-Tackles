// Display-only — the amount actually charged/verified lives in the
// khalti-initiate-lead-payment / khalti-confirm-lead-unlock edge functions'
// LEAD_FEE_PAISA secret, and must be kept in sync with this by hand.
export const LEAD_FEE_NPR = 99;
export const LEAD_FEE_REGULAR_NPR = 100;
export const LEAD_FEE_PAISA = 9900;
